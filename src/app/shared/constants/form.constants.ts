/** Default placeholder for optional dropdowns (value stays null until user selects). */
export const SELECT_PLACEHOLDER = 'Select Option';

/** Standard file upload prompt across the portal. */
export const UPLOAD_PLACEHOLDER = 'Click to upload';

/** Max length for person name fields (first / middle / last). */
export const PERSON_NAME_MAX_LENGTH = 50;

/** Max length for teacher bank name. */
export const BANK_NAME_MAX_LENGTH = 50;

/** Max length for student previous school name. */
export const PREV_SCHOOL_MAX_LENGTH = 255;

/** Max length for student previous class passed. */
export const PREV_CLASS_MAX_LENGTH = 50;

/** Max length for student TC number. */
export const TC_NUMBER_MAX_LENGTH = 255;
