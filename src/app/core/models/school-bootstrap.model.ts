export interface SchoolBootstrap {
  id: string;
  name: string;
  subdomain: string;
  shortName?: string;
  tagline?: string;
  logoUrl?: string;
  faviconUrl?: string;
  primaryColor: string;
  secondaryColor?: string;
  accentColor?: string;
  textOnPrimary?: string;
  schemaName: string;
}
